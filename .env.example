# =============================================================================
# FamilyVault - Environment Variables
# Copy this file to .env and customize as needed
# =============================================================================

# --- Server ---
SECRET_KEY=change-me-to-a-random-64-char-string

# --- PostgreSQL ---
# To use your own PostgreSQL instance, change these and remove the postgres
# service from docker-compose.yml
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=familyvault
POSTGRES_PASSWORD=familyvault
POSTGRES_DB=familyvault

# --- MinIO / S3 ---
# To use your own MinIO or AWS S3, change these and remove the minio
# service from docker-compose.yml
S3_ENDPOINT_URL=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=familyvault
S3_REGION=us-east-1

# --- Frontend ---
API_URL=http://localhost:8000/api

# --- CORS ---
CORS_ORIGINS=["http://localhost:3000"]

# --- Email (optional) ---
# Leave SMTP_HOST empty to disable email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@familyvault.local
SMTP_USE_TLS=true
